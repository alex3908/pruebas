<%= render layout: "components/mailer_block" do %>
  <h1><span style="font-size:25px">Cambiar Contraseña</span></h1>
  &nbsp;
  <p>
    <span>Hola <%= @resource.email %>:</span></p>
  <p>
    <span>
      Alguien ha solicitado un enlace para cambiar su contraseña. Puede hacerlo a través del siguiente enlace.
    </span>
  </p>
<% end %>

<%= render partial: "components/mailer_button", locals: { title: "Cambiar mi contraseña", url: edit_password_url(@resource, reset_password_token: @token) } %>

<%= render layout: "components/mailer_block" do %>
  <p>
    <span>
      Si no solicitó esto, ignore este correo electrónico.
      <br>
      Su contraseña no cambiará hasta que acceda al enlace de arriba y cree una nueva.
    </span>
  </p>
<% end %>
